# Personal Portfolio

A responsive React portfolio built with Vite. It includes animated sections for the hero, about, technology stack, projects, and contact details.

## Getting Started

Install the dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Then open the local URL shown in the terminal.

## Scripts

- `npm run dev` starts the Vite development server.
- `npm run build` creates a production build.
- `npm run preview` serves the production build locally.
- `npm run lint` checks the source files with ESLint.

## Current Notes

- The navigation links are centered on desktop screens.
- The contact form is currently disabled; social contact links remain available.

## Build

Create a production build with:

```bash
npm run build
```